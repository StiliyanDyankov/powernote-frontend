import { useState } from "react";
import Button from "@mui/material/Button";

function App() {
    const [count, setCount] = useState(0);

    return (
		<div className="App">
			haiaia
			<p className="font-bold text-red-500 underline">fkjfdk</p>
			<Button variant="contained" className="bg-black">Hello</Button>
		</div>
	);
}

export default App;
